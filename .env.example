# Anthropic API key for LPCA narrative synthesis (src/llm_synthesis.py).
# Model used: claude-haiku-4-5. Get a key at https://console.anthropic.com/
#
# Optional: if unset (or the API call fails for any reason), the app falls
# back to a deterministic template-based narrative built from the same
# structured data — the dashboard is never non-functional without this key.
ANTHROPIC_API_KEY=
